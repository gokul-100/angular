import { AreaofsquarePipe } from './areaofsquare.pipe';

describe('AreaofsquarePipe', () => {
  it('create an instance', () => {
    const pipe = new AreaofsquarePipe();
    expect(pipe).toBeTruthy();
  });
});
